import Button from './components/Buttons'

function App() {

  return (
    <> {/*because react needs a single parent element*/}
      <Button text="Users" />
      <Button text="Policies" />

    </>
  )
}

export default App
